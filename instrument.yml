name: Lizard
run: once
results:
  - dir: ${instrument}/results
commands:
  - name: Run Lizzard
    win: python lizard.py -V --csv ${repo} > ${instrument}/results/${repoName}.csv
    unix: python3 lizard.py -V --csv ${repo} > ${instrument}/results/${repoName}.csv
